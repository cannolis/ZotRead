<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
  <!-- ZotRead "Z" mark — single-path vector so Zotero's sidenav mask
       renders it correctly with currentColor (white in dark mode,
       dark in light mode). The raster favicon.svg becomes a flat
       blob when used inside a mask. -->
  <path fill="currentColor" d="M5 4 H19 V7 L9 17 H19 V20 H5 V17 L15 7 H5 Z"/>
</svg>
